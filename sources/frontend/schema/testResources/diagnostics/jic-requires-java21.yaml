product: jvm/lib

settings:
  java:
    compileIncrementally: <![Error] Java incremental compilation requires JDK 21 or higher, but this module uses JDK 17.!>true<!>
  jvm:
    jdk:
      version: <![Error] Java incremental compilation requires JDK 21 or higher, but this module uses JDK 17.!>17<!>