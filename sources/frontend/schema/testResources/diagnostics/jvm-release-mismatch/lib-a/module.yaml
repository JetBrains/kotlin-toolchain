product:
  type: kmp/lib
  platforms: [ android, jvm, iosArm64, iosSimulatorArm64, iosX64, linuxArm64, linuxX64, macosArm64, mingwX64 ]

dependencies:
  - <![Warning] Module `lib-a` uses JVM release 11 but depends on module `lib-b` with a higher JVM release 25 (default)!>../lib-b<!>
  - <![Warning] Module `lib-a` uses JVM release 11 but depends on module `lib-c` with a higher JVM release 26!>../lib-c<!>

settings:
  jvm:
    release: <![Warning] Module `lib-a` uses JVM release 11 but depends on module `lib-b` with a higher JVM release 25 (default)!><![Warning] Module `lib-a` uses JVM release 11 but depends on module `lib-c` with a higher JVM release 26!>11<!><!>