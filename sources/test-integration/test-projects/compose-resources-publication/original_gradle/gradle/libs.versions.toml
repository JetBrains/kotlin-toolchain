[versions]
# Keep in sync with the versions the Kotlin Toolchain uses by default (see DefaultVersions.kt), so that the
# publications of this Gradle project and of the Amper modules next to it can be compared meaningfully.
kotlin = "2.4.10"
compose = "1.11.1"

[libraries]
compose-runtime = { module = "org.jetbrains.compose.runtime:runtime", version.ref = "compose" }
compose-components-resources = { module = "org.jetbrains.compose.components:components-resources", version.ref = "compose" }

[plugins]
kotlinMultiplatform = { id = "org.jetbrains.kotlin.multiplatform", version.ref = "kotlin" }
composeCompiler = { id = "org.jetbrains.kotlin.plugin.compose", version.ref = "kotlin" }
composeMultiplatform = { id = "org.jetbrains.compose", version.ref = "compose" }
