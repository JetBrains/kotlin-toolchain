product: jvm/app

settings:
  # No specific Kotlin version here, because the lowest supported JDK should support the default Kotlin version
  jvm:
    jdk:
      version: {{MIN_JDK_VERSION}} # should always be the lowest supported JDK
