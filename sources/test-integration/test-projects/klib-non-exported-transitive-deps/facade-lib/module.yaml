product:
  type: kmp/lib
  platforms: [ js, jvm, wasmJs, wasmWasi ]

# Neither of these is exported: neither 'Model' nor the kotlinx-datetime types may be visible to the consumers
dependencies:
  - ../model-lib
  - org.jetbrains.kotlinx:kotlinx-datetime:0.7.1
