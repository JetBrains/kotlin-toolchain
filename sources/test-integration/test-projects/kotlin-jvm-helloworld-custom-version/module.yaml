product: jvm/app

settings:
  kotlin:
    # Replaced dynamically in the tests
    version: {{KOTLIN_COMPILER_VERSION}}
  jvm:
    jdk:
      # Replaced dynamically in the tests
      version: {{JDK_VERSION}}
